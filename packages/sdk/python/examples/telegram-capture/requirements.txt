# Local example only — NOT part of Cerid's main dependency graph.
# Do NOT add these to src/mcp/requirements*.txt or requirements.lock.
#
# Install into a separate venv:
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt

python-telegram-bot==22.8
httpx>=0.27,<1.0
