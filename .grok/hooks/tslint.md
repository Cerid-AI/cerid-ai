# TypeScript Lint Hook (Grok Build)

Run on TS/TSX changes.

- Run `eslint` (or the project's lint script)
- Enforce cerid frontend coding standards and design system usage
- Combine with `tscheck.md` for full static analysis

Grok equivalent of the `tslint.sh` hook.
