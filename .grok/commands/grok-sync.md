Manage Cerid AI knowledge base sync (Grok Build version).

Accept an argument:
- `status`, `export`, `import`

This is the Grok-native equivalent of the Claude `/sync` command. Uses `scripts/cerid-sync.py`.
