# Per-file test-antipattern baseline — SHRINK-ONLY.
#
# 2026-08-02: total moved 147 -> 148, and two files' baselines ROSE
# (test_simulated_sessions 7 -> 22, test_e2e_pipeline 7 -> 8). That is NOT new
# debt. TA005 was added because TA004's detector was import-shaped and could
# only see `patch(...)` + `from X import Y`; it was blind to the far more
# common `result = await mock_aq(...)` form, where the test calls the mock
# directly and asserts on the fixture it just supplied. Those 16 tautologies
# were always there — burning down the 13 originally-visible TA004 sites would
# have lowered this file and certified test_simulated_sessions.py while ~60%
# of it still asserted nothing.
#
# Genuinely fixed in the same pass: TA001 9 -> 0, TA002 2 -> 0, TA003 123 ->
# 120, TA004 13 -> 12. So 15 real fixes landed while 16 pre-existing defects
# became visible for the first time. The ruler got better, not the code worse.
# Regenerate after burn-down: python scripts/lint-test-antipatterns.py --update
#
# These rules were written after the fact (2026-07-30 graduation sweep),
# so the suite carries a residue. A new test file has an implicit
# baseline of 0, so the gate is blocking for new code while the residue
# burns down opportunistically. Counts can never climb.
#
# SEED THIS IN cerid-ai-internal ONLY. The public mirror strips test
# files, so it reports a LOWER count and --check passes there; running
# --update in public would bake that lower number in and make internal
# fail against its own tree.
#
# Highest-value burn-down targets first: TA004 (the tautology — the test
# asserts the fixture it supplied) then TA003 (blanket config mock —
# silently stops the code under test from running). TA001/TA002 are
# order-dependence hazards, cheap to fix.
# total=148 files=21 [TA003×120, TA004×12, TA005×16]
9	src/mcp/tests/test_archive.py
15	src/mcp/tests/test_contextual.py
7	src/mcp/tests/test_curator.py
8	src/mcp/tests/test_e2e_pipeline.py
3	src/mcp/tests/test_ingestion.py
3	src/mcp/tests/test_kb_admin.py
14	src/mcp/tests/test_maintenance.py
2	src/mcp/tests/test_memory.py
2	src/mcp/tests/test_memory_agent.py
3	src/mcp/tests/test_memory_consolidation.py
23	src/mcp/tests/test_neo4j_expanded.py
2	src/mcp/tests/test_nli_verification.py
9	src/mcp/tests/test_query_agent.py
2	src/mcp/tests/test_rag_resilience.py
7	src/mcp/tests/test_rectify.py
1	src/mcp/tests/test_relevance_floor_scale.py
1	src/mcp/tests/test_sdk_contract.py
2	src/mcp/tests/test_sdk_endpoints.py
22	src/mcp/tests/test_simulated_sessions.py
11	src/mcp/tests/test_sync.py
2	src/mcp/tests/test_webhooks.py
