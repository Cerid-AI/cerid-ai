{
    "name": "ocr",
    "version": "1.0.0",
    "type": "parser",
    "tier": "pro",
    "description": "OCR support for scanned PDFs and image-only documents. Uses Docling (primary) with Tesseract fallback.",
    "requires": ["docling"],
    "config": {
        "OCR_ENGINE": {
            "description": "OCR engine to use: 'docling' (recommended) or 'tesseract'",
            "default": "docling"
        },
        "OCR_MIN_TEXT_PER_PAGE": {
            "description": "Minimum characters per page before OCR triggers (below this = likely scanned)",
            "default": 50
        }
    }
}
