# Evaluation-only dependencies — NOT required for production.
# Install with: pip install -r requirements-eval.txt
# Used by: make test-eval, RAGAS benchmarks, Monte Carlo harness
ragas>=0.2,<0.5
scipy
